console.log("Hola mundo ..");

const showUserInfo = (userName, userAge) => {
  return `El nombre del usuario es ${userName} y la edad es ${userAge}`;
};

const user1 = showUserInfo("Dennys Ferrer", 34);
console.log(user1);
